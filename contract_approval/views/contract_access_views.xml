<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="contract_access_view_tree" model="ir.ui.view">
        <field name="name">contract.access.tree</field>
        <field name="model">contract.access</field>
        <field name="arch" type="xml">
            <tree string="Access" editable="bottom">
                <field name="contract_id" invisible="1"/>
                <field name="user_ref" options="{'no_create': True}"/>
            </tree>
        </field>
    </record>
</odoo>
